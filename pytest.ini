[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = backend
addopts = -ra --strict-markers
markers =
    db: requires a PostgreSQL server (ASM_TEST_DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning:passlib.*
