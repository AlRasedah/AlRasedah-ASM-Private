"""Exteriq ASM sensor framework.

Scanners are *sensors*: replaceable components that observe the internet-facing
attack surface and report what they saw using a scanner-independent observation
schema (:mod:`asm_sensors.observations`). The platform never consumes a
scanner's native output format directly.
"""

__version__ = "0.1.0"
